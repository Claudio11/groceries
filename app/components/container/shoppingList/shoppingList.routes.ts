import { Routes } from '@angular/router';

import { ShoppingListDetailComponent } from './shoppingListDetail.component';

// Route Configuration
export const ShoppingListRoutes: Routes = [
  { path: 'shoppingList', component: ShoppingListDetailComponent }
];
